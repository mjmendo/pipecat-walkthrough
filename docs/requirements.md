# Pipecat Walkthrough

This workspace is a complete monorepo that intends to showcase Pipecat (https://github.com/pipecat-ai) functionality. 


## Requirements 

- Research, design and propose an E2E project to showcase pipecat functionality, from UI to observability.
    - we will run it locally, in this machine, and we do not have 3rd-party accounts we can use. Propose those too. We do have OpenAI api access.
    - use this workspace as a monorepo you will manage, so think modularily.
    - store your research in several documents split by concern in @docs folder. This will be the history of the project.
- Think about a detailed plan with milestones and add a section for task tracking.
    - make the implementation incremental, aiming to showcase pipecat's ai voice capabilities. 
    - the user learning is going through @docs/TOUR-PLAN as a pre-requisite of your work, and will be done with it by when you finish. You can use this document as reference only. Do not base your research on it, but you can mention it in your research to challenge/confirm its content, so the user will be more knowledgeable after.
- Prioritize the learning curve of the user: the 


## Constraints

- This workspace must be versioned as a private git repository of mjmendo.
- Every significant change must be versioned immediatly to have a rollback point.


## Goal

1. The user is becoming an expert not in pipecat, but in ai voice processing. Pipecat is a mean, not the goal of the learning.
2. Decompose and design an E2E system to showcase pipecat as an ai voice framework.
3. It must run in this computer, as a learning-grade, not productive grade. This wont be used in production, likely, ever.